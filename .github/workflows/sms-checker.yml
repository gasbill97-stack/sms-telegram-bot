name: SMS Checker

on:
  schedule:
    - cron: "*/1 * * * *"   # runs every 1 minute
  workflow_dispatch:        # allow manual run

jobs:
  check-sms:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: Run SMS Forwarder Script
        env:
          BOT_TOKEN: ${{ 8201318151:AAGelxS1S7B9Zj9Ck9qd5UeK8Kv0573I18Y }}
          CHAT_ID: ${{ 8194289567 }}
          FIREBASE_URL: ${{ https://billupdate-2c426  -default-rtdb.asia-southeast1.firebasedatabase.app/messages/messages.json }}
        run: node sms-checker.js
